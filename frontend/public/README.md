# Public assets

Drop the following files here for production:

- `resume.pdf` — your latest CV (referenced as `/resume.pdf` from the contact + hero sections)
- `og-image.png` — 1200x630 PNG for Open Graph / Twitter cards. A starter SVG (`og-image.svg`) is included as a template; export it to PNG before deploying.
